class ApiEndpoints {
  static const baseUrl = "https://YOUR_DOMAIN_OR_IP/api";

  // Speech
  static const prompts = "/prompts";
  static const submitAttempt = "/attempts";

  // Progress
  static const progress = "/progress";
}
